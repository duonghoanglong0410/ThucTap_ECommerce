@model TT_ECommerce.Models.UserProfileViewModel

<h2>User Profile</h2>

<div>
    <h4>Username: @Model.UserName</h4>
    <h4>Email: @Model.Email</h4>
    <!-- Display other properties as needed -->
</div>
